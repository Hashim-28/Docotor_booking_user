// Specialty model — color data is handled in the view layer
class Specialty {
  final String id;
  final String name;
  final String iconName;

  const Specialty({
    required this.id,
    required this.name,
    required this.iconName,
  });
}
